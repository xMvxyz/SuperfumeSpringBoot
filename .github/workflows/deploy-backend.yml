name: Deploy Backend to EC2

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: Copy JAR to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_KEY }}
          source: "target/Superfume-0.0.1-SNAPSHOT.jar"
          target: "/home/ec2-user/app/Superfume-backend.jar"
          use_insecure_cipher: true

      - name: Deploy and restart backend on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_KEY }}
          use_insecure_cipher: true
          script: |
            mkdir -p /home/ec2-user/app
            
            pkill -f "Superfume-backend.jar" || true
            sleep 2
            
            java -version
            
            cd /home/ec2-user/app
            nohup java -jar Superfume-backend.jar > app.log 2>&1 &
            
            sleep 5
            
            if pgrep -f "Superfume-backend.jar" > /dev/null; then
              echo "Backend desplegado y ejecutándose correctamente"
            else
              echo "Error: El backend no pudo iniciarse"
              tail -n 50 app.log
              exit 1
            fi
